<div class="w-full">
  <div>
    <%= form_with url: posts_url, method: :get do |f| %>
      <%= f.text_field :query %>
      <%= f.submit "Search" %>
    <% end %>
  </div>
  <div id="posts" class="grid min-w-full gap-2 p-4 md:grid-cols-2 lg:grid-cols-3">
    <%= render @posts %>
  </div>
</div>
